対応方針:

- リポジトリの README.md トップに指定のヘッダー画像を表示します。
- 現状のSVGヘッダーコメントを削除し、<img>タグで画像を埋め込む。
- 画像URLにはIssueの添付画像（GitHub user-attachments）を使用し、幅を100%に設定。
- 影響範囲はREADMEのみ。

作業内容:

1. README.mdの該当コメント削除および<img>タグ追加
2. 変更を反映したPRを作成
3. CI/プレビューで表示確認

以上、対応を開始します。
